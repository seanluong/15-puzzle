describe('Unit: Test Jasmine up and running with Karma', function() {
	beforeEach(function() {
		
	});

	it("should do nothing", function() {
		expect(true).toBeTruthy();
		expect(false).toBeFalsy();
		expect(null).toBeNull();
	});
});